<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.org.alan.exam.mapper.ExamQuAnswerMapper">
    <resultMap id="ListResultMap"
               type="cn.org.alan.exam.model.vo.exam.ExamQuAnswerExtVO">
        <result column="gradeId" property="gradeId" />
        <result column="sort" property="sort" />
        <result column="image" property="image" />
        <result column="content" property="content" />
    </resultMap>
    <delete id="deleteByUserIds">
        delete from t_exam_qu_answer where user_id in
        <foreach collection="userIds" item="userId" open="(" close=")" separator=",">
            #{userId}
        </foreach>
    </delete>
    <select id="list" resultMap="ListResultMap">
    SELECT op.`gradeId`,eq.`sort`,op.'content',op.'image'
    FROM t_exam_question eq
    LEFT JOIN t_option op ON eq.question_id=op.qu_id
    WHERE op.exam_id=#{examId} AND op.question_id=#{questionId}
    ORDER BY eq.sort ASC
    </select>
    <!-- 获取单题作答信息 -->
    <select id="questionAnalyse" resultType="cn.org.alan.exam.model.vo.score.QuestionAnalyseVO">
        select count(case where is_right = 1 then 1 end) as rightCount, count(*) as totalCount
        from t_exam_qu_answer
        where exam_id = #{examId} and question_id = #{questionId}
    </select>
</mapper>
